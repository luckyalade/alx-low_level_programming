#include <stdio.h>

/**
 * main - print a string of code with printf
 *
 * Return: Always 0
 */
int main(void)
{
        printf(" with proper grammar, but the outcome is a piece of art,");
        return (0);
}
